<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <TextView
        android:id="@+id/item_no_due_date_text_view_dark"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:paddingTop="8dp"
        tools:paddingBottom="4dp"
        android:text="@string/no_due_date"
        android:textColor="@color/white"
        android:textSize="16sp"
        android:textStyle="bold" />

    <!-- We have to wrap the separator because there is no set margin method on Remote Views below api 31-->
    <FrameLayout
        android:id="@+id/no_due_date_separator_wrapper_dark"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="6dp"
        tools:visibility="gone">

        <LinearLayout
            android:id="@+id/no_due_date_separator_dark"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:alpha="0.2"
            android:background="@color/white"
            android:orientation="horizontal" />

    </FrameLayout>

</LinearLayout>
